version: 3.0.{build}
branches:
  only:
    - master
image:
  - macos-monterey
  - Visual Studio 2022
  - Ubuntu2204
platform: x64
environment:
  nodejs_version: "22"
install:
  - sh: npm install -g yarn
build_script:
  - sh: if [ "$env:OS" != "Windows_NT" ]; then yarn dist; fi
  - ps: if ($env:OS -eq "Windows_NT") { yarn dist }
artifacts:
  - path: dist/*.exe
  - path: dist/*.dmg
  - path: dist/*.AppImage
test: false
